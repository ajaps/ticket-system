<main class="login-form devise-page">
  <div class="cotainer">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header">Sign up</div>
            <div class="card-body">
              <%= render "devise/shared/error_messages", resource: resource %>

              <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                <div class="form-group row">
                  <%= f.label :user_name, class: 'col-md-4 col-form-label text-md-right' %><br />
                  <%= f.email_field :name, autofocus: true, autocomplete: "name", class: 'form-control col-md-6' %>
                </div>

                <div class="form-group row">
                  <%= f.label :email, class: 'col-md-4 col-form-label text-md-right' %><br />
                  <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control col-md-6' %>
                </div>

                <div class="form-group row">
                  <%= f.label :password, class: 'col-md-4 col-form-label text-md-right' %><br />
                  <%= f.password_field :password, autocomplete: "new-password", class: 'form-control col-md-6' %>
                </div>

                <div class="form-group row">
                  <%= f.label :password_confirmation, class: 'col-md-4 col-form-label text-md-right' %><br />
                  <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control col-md-6' %>
                </div>

                <div class="actions form-group row">
                  <div class='offset-md-6'>
                    <%= f.submit "Sign up", class: 'btn btn-primary' %>
                  </div>
                </div>
              <% end %>

              <%= render "devise/shared/links" %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>
